export interface MenuItemType {
  _id: string;
  title: string;
  category: string;
  description: string;
  price: number;
  imageUrl: string;
}

export interface userType {
  id: string;
  name: string;
  email: string;
}
